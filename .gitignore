node_modules/
dist/
.env
.env.local

# Signing keys must never be committed
*.keystore

# Local scratch files
/test.*
/dup_name_state.json
/*.jpg

# Personal data — the app reads this from pCloud Photos/, never from the repo
/faces.json

# rclone mount point for tools/ scripts (make mount) — never repo content
/Photos/

# Python bytecode cache
__pycache__/
*.pyc

# Resumable-run state files written by tools/*.py
/*_state.json
